import java.util.Scanner;

public class Metods {
    Scanner sc = new Scanner(System.in);

   /* void verifyIntType(int opc){
        if (sc.hasNextInt()) {
            // Si la entrada es un entero
            opc = sc.nextInt();
        } else {
            // Si la entrada no es un entero
            System.out.println("La entrada no es un número entero válido.");

        }
    }*/



}
